import request from '../../utils/request.js'
const state = {
	takeFoodList:[]
}
const mutations = {
	SET_TAKEFOODLIST(state, takeFoodList) {
		state.takeFoodList = takeFoodList
	},
	
}
const actions = {
	
}
const getters = {}


export default {
	state,
	mutations,
	actions,
	getters,
}
